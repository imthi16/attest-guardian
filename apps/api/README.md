# Attest API

FastAPI application for the versioned Attest Guardian API. Public feature routes belong
under `app/api/v1`; route functions stay thin and delegate business logic to services.

Run from the repository root with `make dev-api`. See `docs/DEVELOPMENT.md` for setup and
verification commands.
